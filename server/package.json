{
  "name": "summoner-realms-peerserver",
  "version": "1.0.0",
  "private": true,
  "description": "Optional self-hosted PeerJS signaling server for Summoner Realms multiplayer. Not required to play — the game uses the public PeerJS broker by default.",
  "type": "commonjs",
  "scripts": {
    "start": "node peer-server.js"
  },
  "dependencies": {
    "peer": "^1.0.2"
  }
}
