{
  "name": "summoner-realms",
  "version": "1.0.0",
  "private": true,
  "description": "Root delegator so Render builds/starts the multiplayer backend in server/ even when the service's Root Directory is left at the repo root. (Cleaner alternative: set Render's Root Directory to `server`.) This file does NOT affect the static GitHub Pages frontend.",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "postinstall": "npm install --prefix server",
    "start": "node server/server.js",
    "check:worldgen": "node tools/worldgen-check.mjs 40"
  }
}
