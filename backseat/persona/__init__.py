"""Personality system — PLAN step 4 (not built yet).

Personas are user-editable JSON files (schema.py / store.py / prompt.py /
builtin/*.json). The DEFAULT_PERSONALITY constant in app.py moves here as
builtin/mine-buddy.json.
"""
